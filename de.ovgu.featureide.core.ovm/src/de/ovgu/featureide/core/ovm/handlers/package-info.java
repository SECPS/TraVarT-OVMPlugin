/**
 * This package contains the abstract transformation handlers which are used by interface. In the interface the concrete implementations are referenced.
 */
package de.ovgu.featureide.core.ovm.handlers;
