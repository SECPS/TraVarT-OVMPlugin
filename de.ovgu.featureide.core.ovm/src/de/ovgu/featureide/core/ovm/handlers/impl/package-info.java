/**
 * This package contains the concrete implementations of the handlers referenced in the user interface. There currently exits an transformation handler for
 * importing OVMs (transforming OVM to Feature Model) and for exporting OVMs (transforming Feature Model to OVM).
 */
package de.ovgu.featureide.core.ovm.handlers.impl;
