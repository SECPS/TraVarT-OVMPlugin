/**
 * This package contains the concrete implementations of the transformation handlers. Currently two transformation handlers exit: one importing OVMs
 * (transforming OVM to Feature Model) and another for exporting OVMs (transforming Feature Model to OVM). These handlers will be called from the user
 * interface.
 */
package de.ovgu.featureide.core.ovm.handlers.impl;
